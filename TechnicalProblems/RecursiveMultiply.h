#pragma once
#ifndef _RECURSIVE_MULTIPLY_H_
#define _RECURSIVE_MULTIPLY_H_

/**
 * Write a recursive function to multiply two positive integers without using the
 * '*' operator or '/' operator. You can use addition, subtraction, and bit
 * shifting, but you should minimize the number of those operations.
 */
class RecursiveMultiply
{
public:

	int recursiveMultiply(int n1, int n2)
	{

	}
};

#endif	// _RECURSIVE_MULTIPLY_H_
#pragma once
#ifndef _ROTATE_IMAGE_H_
#define _ROTATE_IMAGE_H_

#include "pch.h"

/**
 * Problem: You are given a n x n 2D matrix representing an image. Rotate the 
 * image by 90 degrees clockwise.
 *
 * Note: You must rotate the image in-place, which means that you need to modify
 * the input directly.
 */
class RotateImage
{
public:
	void rotate(std::vector<std::vector<int>> &matrix)
	{

	}
};

#endif	// _ROTATE_IMAGE_H_
#pragma once
#ifndef _PRINT_JSON_FORMAT_STRING_H_
#define _PRINT_JSON_FORMAT_STRING_H_

/**
 * Problem: Suppose we are given a string that contains a JSON object, but in string
 * form. Print out of the contents of the string in a formatted form similar to
 * how a JSON object looks.
 */
class PrintJSONFormatString
{
public:

	
};

#endif	// _PRINT_JSON_FORMAT_STRING_H_